= render "./shared/header"
.mypage-wrapper
  .profile-content
  = form_with(model: @user, url: '/new', local: true, class: 'profile-contents') do |f|
    .profileTitle
      プロフィール
    .profileImage
      .profileImage__icon
        .profileImage__icon--image
        .profileImage__icon--name
          = f.text_field(:hoge, placeholder: '例)AYA☆セール中')
    .profileMessage
      .profileMessage__text
        = f.text_area(:hoge, placeholder: '例）こんにちは☆ ご覧いただきありがとうございます！かわいいものやきれいめオフィスカジュアル中心に出品しています。服のサイズは主に9号です。気持ちよくお取引できるよう心がけていますので、商品や発送方法などご質問がありましたらお気軽にどうぞ♪')
      .profileMessage_button
        = f.submit("変更する", {class: 'btn'})
  = render './shared/sideBar'
= render './shared/footer'
