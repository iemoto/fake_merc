-# %h2 Sign up
-# = form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f|
-#   = render "devise/shared/error_messages", resource: resource
-#   .field
-#     = f.label :nick_name
-#     %br/
-#     = f.text_field :nick_name, autofocus: true, require: true, autocomplete: "nick_name"
-#   .field
-#     = f.label :email
-#     %br/
-#     = f.email_field :email, autofocus: true, autocomplete: "email"
-#   .field
-#     = f.label :password
-#     - if @minimum_password_length
-#       %em
-#         (#{@minimum_password_length} characters minimum)
-#     %br/
-#     = f.password_field :password, autocomplete: "new-password"
-#   .field
-#     = f.label :password_confirmation
-#     %br/
-#     = f.password_field :password_confirmation, autocomplete: "new-password"
-#   .actions
-#     = f.submit "Sign up"
-# = render "devise/shared/links"
-# ↑↑↑本来は不要ですが、コメント化したものは念のため残しております。最終的には調整する。


.done-bread-header
  = link_to "https://www.mercari.com/jp/", class: "done-bread-header__link" do
    = image_tag asset_path('fakelogo.svg'), class: "done-bread-header__link--logo", alt: "mercari", size:"134x36"
  .done-bread-header-right
    %ol.clearfix
      %li
        会員情報
        .progress-status__mem
      %li
        電話番号認証
        .progress-status__num
      %li.active
        完了
        .progress-status__active

.p-done
  .p-done-container
    .p-done-header
      会員登録完了
    .p-done-forms
      .p-done-content
        %p.coment ありがとうござます。
        %p.coment 会員登録が完了しました。
    .p-done-btn
      = link_to "メルカリをはじめる", '/', class: "p-done-btn__link"
= render './shared/simple-footer'